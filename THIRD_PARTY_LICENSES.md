# Third-party licenses

This file is generated automatically by
[`.github/workflows/license-check.yml`](.github/workflows/license-check.yml)
on every push to `main`. Do not edit it by hand — the next CI run
will overwrite manual changes.

Beyond the Python dependencies listed below, the container image
bundles the statically linked [rclone](https://rclone.org/) binary
(MIT License), and the service talks to an
[Apache Tika](https://tika.apache.org/) server (Apache License 2.0)
that runs as a separate container image and is not vendored here.

The list below covers the runtime Python dependencies bundled with
the `src/` service. Development-only dependencies are excluded.

*The dependency table is generated by the first CI run on `main`.*
